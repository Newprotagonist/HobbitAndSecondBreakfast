<nav class="navbar navbar-expand-lg">
  <div class="container-fluid">
    <%= link_to root_path, class: "navbar-brand" do %>
      <%= image_tag 'icons/magician-icon-one.png', height:50 %>
    <% end %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-lg-0">
        <li class="nav-item dropright">
          <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            Log
          </a>
          <ul class="dropdown-menu dropright"  aria-labelledby="navbarDropdown">
            <% if user_signed_in? %>
              <%= link_to "Sign-out", destroy_user_session_path, data: {turbo_method: :delete}, class:"dropdown-item" %>
            <% else %>
              <%= link_to "Sign-in", new_user_session_path, class:"dropdown-item" %>
              <%= link_to "Sign-up", new_user_registration_path, class:"dropdown-item" %>
            <% end %>
          </ul>
        </li>
      </ul>
      <ul class="navbar-nav mb-lg-0">
        <% if user_signed_in? %>
          <% if current_user.hobbit %>
            <li class="nav-item">
            <% if current_user.offer %>
              <%= link_to "Edit my Offer", edit_offer_path(current_user.offer), class:"nav-link" %>
            <% else %>
              <%= link_to "Create an Offer", new_offer_path, class:"nav-link" %>
            <% end %>
            </li>
          <% end %>
        <% end %>
        <li class="nav-item">
          <%= link_to "Offers", offers_path, class:"nav-link" %>
        </li>
        <li class="nav-item">
          <%= link_to "My reservations", reservations_path, class:"nav-link" %>
        </li>
      </ul>
    </div>
  </div>
</nav>
